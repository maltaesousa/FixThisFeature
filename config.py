# The table name that will contain the issues
editTableName = "fix_issue"
# The field name that will store the feature layer name where the issue is
featureLayerAttribute = "feature_layer"
# The field name that will store the feature id where the issue is
featureIdAttribute = "feature_id"
# The field name that will store the issue description
descriptionAttribute = "description"
